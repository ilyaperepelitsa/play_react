let visibility = false;

const render = () =>
  {
    <div>
      <h1>Visibility toggle</h1>
      <button onClick = >
        {visibility ? "Hide details" : "Show details"}
      </button>
    </div>
  }
